<p id="notice"><%= notice %></p>
<!-- using index instead of show_list from last version -->
<!-- change the table to the bootstrap code -->

<div class = "container overlay">
<table >
 <h2>Subjects</h2>
      <% @subjects.each do |s| %> 
		    <tr>
		      <td colspan="40"> <td><%= link_to image_tag(s.icon, class: "img-responsive icon") %></td>
		      <td align = "left"><%= s.description %></td>
		      <td> <p></p></td>
		     </tr>
		   <% end %>
    
</table>
</div>
<br>

